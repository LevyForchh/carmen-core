table Coord {
    coord: uint32;
    ids: [uint32];
}

table RelevScore {
    relev_score: ubyte;
    coords: [Coord];
}

table PhraseRecord {
    relev_scores: [RelevScore];
}

root_type PhraseRecord;