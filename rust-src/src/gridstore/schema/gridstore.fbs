struct Coord {
    coord: uint32;
    id_list: uint32;
}

table RelevScore {
    relev_score: ubyte;
    coords: [Coord];
}

table IdList {
    ids: [uint32];
}

table PhraseRecord {
    relev_scores: [RelevScore];
    id_lists: [IdList];
}

root_type PhraseRecord;